{
  "name": "evo-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Testes das Firestore Security Rules do Evo Lab, rodados contra o emulador local. Harness Node isolado — NÃO faz parte do pubspec do app Flutter nem do `flutter test`.",
  "scripts": {
    "test": "node --test rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
